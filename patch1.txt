*** Begin Patch
*** Update File: components/widget/WidgetPreview.tsx
@@
-    return (
-      <div
-        className={`flex ${
-          isAI
-            ? "justify-start"
-            : "justify-end"
-        }`}
-      >
-        <div
-          className={`flex max-w-[88%] gap-2.5 ${
-            isAI
-              ? "items-start"
-              : "items-end"
-          }`}
-        >
+    return (
+      <div
+        className={`flex w-full min-w-0 ${
+          isAI
+            ? "justify-start"
+            : "justify-end"
+        }`}
+      >
+        <div
+          className={`flex w-full max-w-[88%] min-w-0 gap-2.5 ${
+            isAI
+              ? "items-start"
+              : "items-end"
+          }`}
+        >
*** End Patch
